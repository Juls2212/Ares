# Open Decisions

Only unresolved matters appear here. Approved Phase 0 decisions are not reopened.

| Decision | Current approved position | Why open | Resolve by | Likely options and impact | Blocks Phase 1 |
| --- | --- | --- | --- | --- | --- |
| Windows PostgreSQL provisioning | PostgreSQL is mandatory; development uses Docker Compose. | Packaging deployment model is not yet chosen. | Before packaging. | Bundled/local managed provisioning vs documented external prerequisite; affects installer, support, and upgrades. | No. |
| Production OpenAI credential storage | Development uses environment variables; `safeStorage` will be evaluated for user-provided credentials. | Settings and packaging flow are later work. | Settings and packaging phases. | `safeStorage`-protected user entry vs another approved secure OS-backed approach; affects migration and recovery. | No. |
| Exact OpenAI model identifiers | OpenAI is planned for interpretation, Speech-to-Text, and Text-to-Speech. | Model availability can change. | Respective AI and voice implementation phases. | Select supported models at implementation time; affects capability, quality, cost, and testing fixtures. | No. |
| Notification recovery after close | Main owns notifications while running. | Background/restart behavior and packaging constraints are undefined. | Notification implementation before packaging. | No recovery, next-launch reconciliation, or an approved platform mechanism; affects reminder expectations. | No. |
| Definitive frontend design | Functional structure is documented; visual design is deferred. | It depends on user-supplied visual references and approval. | Before definitive frontend implementation. | Approved visual models determine layout and styling; affects renderer implementation only. | No. |
| Spoken confirmation after MVP | Visible UI confirmation is mandatory for Level 2 actions. | Spoken confirmation is explicitly optional later work. | After initial MVP, if requested. | Keep visible-only or add an approved voice-confirmation design; affects voice safety validation. | No. |
| Local wake-word engine | Wake-word mode is approved only with fully local detection, disabled by default, and no waiting-mode remote transfer or persistence. | No engine has been selected or installed. Mark LIII is only a conceptual reference and is not a dependency, code source, or architectural authority. | Before voice implementation. | Evaluate Electron and Node.js compatibility, Windows support, fully local inference, Spanish or custom-phrase support, CPU and memory use, false-positive and false-negative behavior, microphone-device compatibility, license and distribution restrictions, offline availability, and maintenance status. | No. |
| Default activation phrase | A configured activation phrase is required before local waiting can begin. | The product phrase has not been approved. | Before voice implementation. | A Spanish fixed phrase vs a more distinctive phrase affects recognition quality, accessibility, and false-positive risk. | No. |
| Activation-phrase configurability | Wake-word mode requires explicit user opt-in. | It is undecided whether the phrase is fixed or user-configurable. | Before voice implementation. | A fixed phrase simplifies testing and recognition tuning; configurable phrases increase user choice but affect engine support, setup, and validation. | No. |
| Minimized-window background behavior | Local waiting is approved while the Ares process is running, including when the primary window is minimized. | The resource-management and UI behavior while minimized are not yet designed. | Before voice implementation. | Continue local waiting while minimized vs pause it under an explicit user control; affects hands-free availability, privacy signaling, and resource use. | No. |
| Wake-word CPU and memory budget | Local detection must be suitable for a Windows desktop application. | No measurable resource budget is approved. | Before voice implementation. | A stricter budget limits engine choices and background impact; a looser budget may improve recognition options but affect battery and responsiveness. | No. |
| False-positive acceptance criteria | Detection must fail closed when state is uncertain and may start only one bounded capture session. | Acceptable false-positive and false-negative rates are not yet defined. | Before voice implementation. | Stricter thresholds reduce unintended capture but can reduce activation reliability; looser thresholds improve responsiveness but increase accidental activation risk. | No. |
| Activation audio feedback | Visible and audible feedback is required after local activation. | The exact sound and accessibility behavior are not yet chosen. | Before voice implementation. | A distinct configurable sound can improve awareness; a silent or reduced-audio option affects accessibility and privacy expectations. | No. |

No additional unresolved issue was discovered during Phase 0 documentation. Recurring events, destructive file behavior, advanced organization classification, and arbitrary application control are excluded rather than open decisions.
